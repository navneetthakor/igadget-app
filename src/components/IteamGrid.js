import React from 'react'

export default function IteamGrid() {
  const {rows, cols} = this.props.
  return (
    <div className='mygrid' style={{gridTemplateColumns: cols, gridTemplateRows: rows}}>
      
    </div>
  )
}
